Status: PARTIAL
Did:
  - Replaced the legacy box-office shape with a strict cross-runtime v3 contract for an exact closed Monday-to-Sunday Worldwide gross period.
  - Made publication fail closed on missing scope, unsafe numbers, future or noncanonical timestamps, cumulative metrics, dependent sources, duplicate canonical URLs, private hosts, and source spread above 25 percent.
  - Added offline fixtures, immutable source-group ownership, atomic durable writes, explicit last-good preservation, and fixture-to-public publication fences across every owner CLI.
  - Removed retired selector pages from rendering, search, Ask, and sitemaps while preserving intentional permanent redirects.
  - Regenerated the static artifacts so the board, sitemap lastmod, and rendered Dataset timestamp are coherent.
Artefacts:
  - engine/fetchers/boxoffice_week_schema.py (600 lines)
  - engine/fetchers/boxoffice_western.py (162 lines)
  - engine/fetchers/run_all.py (440 lines)
  - engine/fetchers/common.py (194 lines)
  - scripts/boxoffice/fetch_boxoffice.py (146 lines)
  - site/lib/boxoffice-schema.mjs (440 lines)
  - site/lib/boxoffice-schema.d.mts (73 lines)
  - site/lib/boxoffice.ts (194 lines)
  - data/cache/fixtures/boxoffice_week_exact.json
  - tests/fixtures/boxoffice/
Verification:
  - python3 -m pytest -q -> PASS: 297 tests passed
  - npm run test:boxoffice -> PASS: 17 tests passed
  - python3 -m compileall -q engine/fetchers scripts/boxoffice scripts/ops tests -> PASS
  - npm run build -> PASS: 5661 static pages and 6839 output files under the 20000-file cap
  - Python and JavaScript parsers -> PASS on the tracked strict v3 data_pending board
  - fixture publication guards -> PASS: all three owner CLIs correctly rejected public-data writes
  - malformed and compact --today values -> PASS: argparse correctly rejected both forms with no traceback
  - generated artifact coherence -> PASS: current timestamp present and stale timestamp count 0
  - retired selector scan -> PASS: absent from build, search, Ask, and sitemaps except intentional redirects
  - npm audit --omit=dev -> BLOCKED, 2 high production-tree advisories in Next.js and PostCSS with only a breaking forced upgrade proposed
  - git diff --check -> PASS
Open issues: The production source registry and live exact-week adapter are deliberately empty, so the public board truthfully remains data_pending. Two upstream production dependency advisories also remain. No deployment or paid provider call was performed.
Next step suggested: register and test at least two approved independent exact-week production sources, then run a reviewed live canary before deployment.
Network URLs:
  - https://registry.npmjs.org
  - https://query.wikidata.org
  - https://github.com
